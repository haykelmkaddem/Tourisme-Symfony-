<form method="post" action="{{ path('even_non_approuve_delete', {'id': even_non_approuve.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ even_non_approuve.id) }}">
    <button class="btn">Delete</button>
</form>
