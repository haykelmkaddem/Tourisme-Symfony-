{% extends 'base.html.twig' %}

{% block title %}Edit Utilisateur{% endblock %}

{% block body %}
    <div class="container">

    <div class="card shadow mb-4 pb-5">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">{{ utilisateur.nom }} {{ utilisateur.prenom }}

        </h6>
    </div>
    <div class="card-body">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-warning">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}
    </div></div></div>
{% endblock %}