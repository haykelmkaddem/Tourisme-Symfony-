<form method="post" action="{{ path('even_popularite_delete', {'id': even_popularite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ even_popularite.id) }}">
    <button class="btn">Delete</button>
</form>
