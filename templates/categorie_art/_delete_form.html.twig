<form method="post" action="{{ path('categorie_art_delete', {'id': categorie_art.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_art.id) }}">
    <button class="btn">Delete</button>
</form>
