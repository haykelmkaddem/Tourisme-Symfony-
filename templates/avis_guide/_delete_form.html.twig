<form method="post" action="{{ path('avis_guide_delete', {'id': avis_guide.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avis_guide.id) }}">
    <button class="btn">Delete</button>
</form>
