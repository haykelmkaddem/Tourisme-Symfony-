{% extends 'backSyrine.html.twig' %}

{% block title %}Edit AvisDestination{% endblock %}

{% block body %}
<div class="container-fluid">

    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">Avis</h6>
        </div>
        <div class="card-body">

    {{ include('avis_destination/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('avis_destination_index') }}">back to list</a>

    {{ include('avis_destination/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
