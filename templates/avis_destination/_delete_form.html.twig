<form method="post" action="{{ path('avis_destination_delete', {'id': avis_destination.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avis_destination.id) }}">
    <button class="btn">Delete</button>
</form>
