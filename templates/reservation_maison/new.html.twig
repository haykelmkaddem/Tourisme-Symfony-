{% extends 'basefront.html.twig' %}

{% block title %}New ReservationMaison{% endblock %}

{% block body %}
    <h1>Create new ReservationMaison</h1>
    {% for message in app.flashes('warning') %}
        <div style="background-color: #cc2255;color: #ffffff; text-align: center" class="alert alert-warning">
            {{ message }}
        </div>
    {% endfor %}

    {{ include('reservation_maison/_form.html.twig') }}

    <a href="{{ path('reservation_maison_index') }}">back to list</a>
{% endblock %}
