<form method="post" action="{{ path('like_dislike_delete', {'id': like_dislike.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ like_dislike.id) }}">
    <button class="btn">Delete</button>
</form>
