{% extends 'base.html.twig' %}

{% block title %}LikeDislike{% endblock %}

{% block body %}
    <h1>LikeDislike</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ like_dislike.id }}</td>
            </tr>
            <tr>
                <th>Aime</th>
                <td>{{ like_dislike.aime ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('like_dislike_index') }}">back to list</a>

    <a href="{{ path('like_dislike_edit', {'id': like_dislike.id}) }}">edit</a>

    {{ include('like_dislike/_delete_form.html.twig') }}
{% endblock %}
