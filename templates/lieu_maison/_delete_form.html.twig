<form method="post" action="{{ path('lieu_maison_delete', {'id': lieu_maison.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lieu_maison.id) }}">
    <button class="btn">Delete</button>
</form>
