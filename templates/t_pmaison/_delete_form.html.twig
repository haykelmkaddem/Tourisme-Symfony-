<form method="post" action="{{ path('t_pmaison_delete', {'id': t_pmaison.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_pmaison.id) }}">
    <button class="btn">Delete</button>
</form>
