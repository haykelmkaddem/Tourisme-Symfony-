<form method="post" action="{{ path('even_approuve_delete', {'id': even_approuve.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ even_approuve.id) }}">
    <button class="btn btn-danger" style="margin-left: 80%; width: 120px;">Delete</button>
    <br><br>
</form>
