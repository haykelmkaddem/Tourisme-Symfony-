{% extends 'backSyrine.html.twig' %}

{% block title %}BadWords{% endblock %}

{% block body %}
    <h1>BadWords</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bad_word.id }}</td>
            </tr>
            <tr>
                <th>Word</th>
                <td>{{ bad_word.word }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bad_words_index') }}">back to list</a>

    <a href="{{ path('bad_words_edit', {'id': bad_word.id}) }}">edit</a>

    {{ include('bad_words/_delete_form.html.twig') }}
{% endblock %}
